/**
 * The distance in chunks that the server will render.
 */
export const RENDER_DISTANCE = 1;

/**
 * The size of a chunk.
 */
export const CHUNK_SIZE = 500;

/**
 * The size of the map.
 */
export const MAP_CHUNKS_WIDTH = 10;

/**
 * Food per chunk.
 */
export const FOOD_PER_CHUNK = 10;

export const FOOD_ORB_SIZE = 3;
